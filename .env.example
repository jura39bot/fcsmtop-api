# ─── EXEMPLE DE CONFIGURATION — copier en .env et adapter ───────────────────
# Ces valeurs sont des EXEMPLES non sensibles. Remplace-les en production.

# Database — PostgreSQL (prod) ou SQLite (dev, défaut si non défini)
DATABASE_URL=postgresql+asyncpg://fcsmtop:CHANGE_ME@localhost:5432/fcsmtop
# Pour dev SQLite (aucune installation requise) :
# DATABASE_URL=sqlite+aiosqlite:///./fcsmtop.db

# API
APP_ENV=development
APP_HOST=0.0.0.0
APP_PORT=8000
SECRET_KEY=CHANGE_ME_IN_PRODUCTION

# Postgres (docker-compose)
POSTGRES_USER=fcsmtop
POSTGRES_PASSWORD=CHANGE_ME
POSTGRES_DB=fcsmtop
